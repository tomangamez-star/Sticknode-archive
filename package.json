{
  "name": "sticknodes-telegram-archive",
  "version": "1.1.0",
  "description": "Render-ready Telegram archive/search bot for public StickNodes.com stickfigure downloads.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node src/index.js",
    "test": "node tests/parser.test.js && node tests/ui.test.js",
    "check": "for f in src/*.js tests/*.js; do node --check \"$f\" || exit 1; done && echo 'ALL SYNTAX OK'",
    "ingest": "node src/ingest.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "node-telegram-bot-api": "^0.66.0",
    "pg": "^8.16.3"
  }
}
